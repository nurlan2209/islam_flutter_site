name: qazaq_republic_classic
description: Классикалық киім үлгілерін сататын Qazaq Republic Classic онлайн дүкені.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.5
  provider: ^6.0.5
  sqflite: ^2.3.0  # Основная SQLite для мобильных
  path_provider: ^2.0.15
  shared_preferences: ^2.1.1
  http: ^1.1.0
  image_picker: ^1.0.4
  flutter_svg: ^2.0.7
  url_launcher: ^6.1.11
  intl: ^0.20.2
  crypto: ^3.0.3
  uuid: ^3.0.7
  path: ^1.8.0  # Для работы с путями в SQLite

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/  # Исправлено: убрал дублирование assets/
    - assets/icons/   # Исправлено: убрал дублирование assets/
  # fonts:
  #   - family: Montserrat
  #     fonts:
  #       - asset: assets/fonts/Montserrat-Regular.ttf
  #       - asset: assets/fonts/Montserrat-Bold.ttf
  #         weight: 700
  #       - asset: assets/fonts/Montserrat-Medium.ttf
  #         weight: 500